import React from "react";
import { StyleSheet, Text } from "react-native";

const SearchScreen = () => {
  return <Text style={{ color: "black" }}>Search</Text>;
};

export default SearchScreen;
const styles = StyleSheet.create({});
